export interface ICourse {
    id: string;
    name: string;
}

export interface ICourseFormValues {
    id: string;
    name: string;
}

export interface ICourseOption {
    key: string;
    text: string;
    value: string;
}

export interface ICourseGroupFormvalues {
    id: string;
}